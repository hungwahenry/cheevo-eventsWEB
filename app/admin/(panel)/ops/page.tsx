import { AdminPlaceholder } from "@/features/admin/components/placeholder"

export default function AdminOpsPage() {
  return (
    <AdminPlaceholder
      title="Ops"
      description="Run-command, queue stats, health checks. Sprint 6 (Analytics + Ops)."
    />
  )
}
