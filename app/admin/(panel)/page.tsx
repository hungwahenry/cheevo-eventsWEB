import { AdminPlaceholder } from "@/features/admin/components/placeholder"

export default function AdminOverviewPage() {
  return (
    <AdminPlaceholder
      title="Overview"
      description="System-wide dashboard lands in Sprint 6 (analytics). For now use the side nav."
    />
  )
}
